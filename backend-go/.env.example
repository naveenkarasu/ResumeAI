# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=8080
DEBUG=true

# PostgreSQL
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=resume_rag
POSTGRES_PASSWORD=password
POSTGRES_DB=resume_rag

# Or use DATABASE_URL for full connection string
# DATABASE_URL=postgres://resume_rag:password@localhost:5432/resume_rag?sslmode=disable

# Qdrant Vector Database
QDRANT_HOST=localhost
QDRANT_PORT=6333

# ML Service (Rust gRPC)
ML_SERVICE_HOST=localhost
ML_SERVICE_PORT=50051

# LLM Backends
LLM_BACKEND=groq

# Groq (free, fast)
GROQ_API_KEY=your-groq-api-key

# OpenAI (optional)
OPENAI_API_KEY=your-openai-api-key

# Anthropic/Claude (optional)
ANTHROPIC_API_KEY=your-anthropic-api-key
